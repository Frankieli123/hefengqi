import type { Metadata } from "next";
import { JsonLd } from "@/components/json-ld";
import { assertLocale, coreContentLocale } from "@/lib/locale";
import { localizedMetadata, webPageSchema } from "@/lib/seo";
type Props = { params: Promise<{ locale: string }> };
const copy = { zh: ["隐私政策", "我们只收集处理询价所需的姓名、联系方式、国家或地区、感兴趣的产品分类和需求信息。询价信息默认保留 24 个月，仅供授权销售及管理员处理。您可以联系我们申请访问、更正或删除。网站统计不采集表单内容或联系方式。"], en: ["Privacy policy", "We collect only the name, contact details, country or region, product category of interest, and requirements needed to process an inquiry. Inquiry data is retained for 24 months by default and is available only to authorized sales staff and administrators. You may request access, correction, or deletion. Website analytics do not collect form content or contact details."], ru: ["Политика конфиденциальности", "Мы собираем только имя, контактные данные, страну или регион, интересующую категорию товара и требования, необходимые для обработки запроса. По умолчанию данные хранятся 24 месяца и доступны только уполномоченным сотрудникам. Можно запросить доступ, исправление или удаление. Аналитика не собирает содержимое форм и контакты."] } as const;
export async function generateMetadata({ params }: Props): Promise<Metadata> { const { locale } = await params; assertLocale(locale); const content = copy[coreContentLocale(locale)]; return localizedMetadata(locale, "/privacy", content[0], content[1]); }
export default async function Page({ params }: Props) { const { locale } = await params; assertLocale(locale); const content = copy[coreContentLocale(locale)]; return <main id="main-content" className="page-shell section-pad"><JsonLd data={webPageSchema(locale, "/privacy", content[0], content[1])} /><article className="mx-auto flex max-w-3xl flex-col gap-8"><h1 className="section-title heading-underlined heading-underlined-left">{content[0]}</h1><p className="text-lg leading-9 text-muted-foreground">{content[1]}</p><p className="text-sm text-muted-foreground">Last updated: 2026-09-07. Verified company contact details must be added before production launch.</p></article></main>; }
